# Architecture Decision Records (ADR)

- **ADR-001**: Use PostgreSQL as the default relational database instead of MongoDB.
- **ADR-002**: Use Go for all new high-throughput microservices instead of Java.
- **ADR-003**: Use gRPC for synchronous inter-service communication instead of REST.
